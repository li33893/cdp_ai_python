"""

知识点 1：transform 解决的问题

.mean() 把每组压成一行。但很多时候你不想压缩，你想让原来每一行都带上自己那组的结果。

同一张表，同一个分组，两种结果：


df = pd.DataFrame({
    "班级": ["一班", "一班", "二班", "二班", "二班"],
    "姓名": ["小林", "小赵", "小周", "小吴", "小郑"],
    "分数": [80, 90, 60, 70, 80],
})

A. 用 .mean()（聚合）


df.groupby("班级")["分数"].mean()
班级
一班    85.0
二班    70.0

5 行 → 2 行，索引变成了班级。

B. 用 .transform("mean")（广播回去）


df.groupby("班级")["分数"].transform("mean")
0    85.0
1    85.0
2    70.0
3    70.0
4    70.0

5 行 → 还是 5 行，索引还是 0~4。每一行填的是"我所在的那个班的平均分"。

只有一个字不同：.mean() 换成 .transform("mean")。差别全在形状上。

	行数	索引	用途
.mean()	组的个数	分组的值	出统计表
.transform("mean")	原表行数	原表索引	加一列回原表



知识点 2：为什么要"形状一样"

因为形状一样，才能直接塞回原表当新列：


df["班级均分"] = df.groupby("班级")["分数"].transform("mean")
   班级  姓名  分数  班级均分
0  一班  小林  80  85.0
1  一班  小赵  90  85.0
2  二班  小周  60  70.0
3  二班  小吴  70  70.0
4  二班  小郑  80  70.0

有了这一列，就能做**"个体 vs 自己组"**的比较：


df["高于均分"] = df["分数"] > df["班级均分"]
   班级  姓名  分数  班级均分   高于均分
0  一班  小林  80  85.0  False
1  一班  小赵  90  85.0   True
2  二班  小周  60  70.0  False
3  二班  小吴  70  70.0  False
4  二班  小郑  80  70.0   True

注意小吴考 70 是 False，小郑考 80 是 True，但如果按全表平均分（76）来比，小吴 70 也是 False、小郑 80 是 True——这里恰好一样。真正的重点是：每一行用的是不同的比较基准（一班用 85，二班用 70）。这是 transform 的核心价值，普通的 df["分数"] > df["分数"].mean() 做不到。

知识点 3：换成 .mean() 会怎样（重要，会静默出错）

df["x"] = df.groupby("班级")["分数"].mean()

不会报错，但结果是：

   班级  分数   x
0  一班  80 NaN
1  一班  90 NaN
2  二班  60 NaN
3  二班  70 NaN
4  二班  80 NaN

全是 NaN。原因：赋值给列的时候 pandas 会按索引对齐。右边那个 Series 的索引是 ["一班", "二班"]，左边 df 的索引是 [0,1,2,3,4]。两边一个都对不上，于是全部填 NaN。

这类 bug 很难发现，因为它不报错。看到某一列莫名其妙全是 NaN，先怀疑索引没对齐。

知识点 4：transform 里能写什么

写法一：字符串（最常用）


df.groupby("班级")["分数"].transform("mean")
df.groupby("班级")["分数"].transform("sum")
df.groupby("班级")["分数"].transform("max")
df.groupby("班级")["分数"].transform("count")   # 每行填"我这组有几行"

写法二：lambda（自定义）

lambda 收到的 s 是一整组的 Series，不是单个值：


d = pd.DataFrame({"g": ["a", "a", "b"], "v": [1, 3, 10]})

d.groupby("g")["v"].transform(lambda s: s - s.mean())
0   -1.0     # a 组均值 2，1-2 = -1
1    1.0     # 3-2 = 1
2    0.0     # b 组只有 10，均值 10，10-10 = 0

这一点和 .apply() 不同——apply 常常是一个元素一个元素处理，transform 里的 lambda 拿到的是整组。

lambda 的返回值有两种情况，都合法：

返回一个标量（如 s.max() - s.min()）→ 自动复制填满整组
返回一个等长 Series（如 s - s.mean()）→ 逐行对应填回去
知识点 5：一个实际场景——按组填缺失值

df = pd.DataFrame({
    "班级": ["一班", "一班", "二班", "二班", "二班"],
    "分数": [80, np.nan, 60, 70, np.nan],
})

df["填补后"] = df["分数"].fillna(df.groupby("班级")["分数"].transform("mean"))
   班级    分数   填补后
0  一班  80.0  80.0
1  一班   NaN  80.0    ← 用一班均分 80 填
2  二班  60.0  60.0
3  二班  70.0  70.0
4  二班   NaN  65.0    ← 用二班均分 65 填

比"全表均值填补"更合理。注意均值计算本身会自动跳过 NaN


"""