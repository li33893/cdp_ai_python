"""
sys.exit() 

ta的作用只有一件事——立刻终止整个程序，后面的代码全部不跑。
import sys

print("这行会打印")
sys.exit()
print("这行永远不会打印")

跑起来只会看到:
这行会打印
第三行虽然写在代码里，但程序在 sys.exit() 那一刻就已经停了，压根不会走到第三行。


第 2 小块:和你已经会的东西对比
你可能会想:这不就是 break 或 return 吗？区别在于停止的范围:

break——只跳出当前这一层循环，循环外面的代码照样跑
return——只结束当前这个函数，函数外面的代码照样跑
sys.exit()——整个程序直接结束，什么都不跑了，不管你在哪一层


打个比方:break 是走出一个房间，return 是走出一栋楼，sys.exit() 是直接把整个园区的电闸拉了。



第 3 小块:括号里的数字是什么
pythonsys.exit(1)
这个 1 叫退出码（exit code）。它不是给你看的，是报告给操作系统的一个信号，说明这次程序是"正常结束"还是"出错结束"。
约定俗成的规则:

sys.exit(0) 或 sys.exit()（不写数字）→ 正常结束（0 = 没毛病）
sys.exit(1) → 出错退出（非 0 = 有问题）

这个数字不会打印在屏幕上，你运行脚本时看不到它。它是留给其他程序用的——比如你写一个自动化脚本，链式跑十个 .py 文件，每跑完一个就检查上一个的退出码，如果是非 0 就知道"上一步出错了，后面别跑了"。
对你现在的项目来说，用不到这套自动化检查，但写 sys.exit(1) 是个好习惯——它明确告诉任何看代码的人（包括你自己）："这里是出错退出，不是正常退出"。


"""

import sys
import os

# def check(n):
#     if n < 0:
#         print("数字是负的，不能处理")
#         sys.exit(1)
#     print(f"处理数字: {n}")

# check(5)
# check(-3)
# check(10)

def load_file(filename):
    if not os.path.exists(filename):
        print("错误：文件不存在")
        sys.exit(1)
    print("文件存在，继续处理")


load_file("不存在的文件.csv")
# 这段代码跑起来，会打印哪几行？（注意 check(-3) 之后会发生什么，check(10) 那行还会不会执行。）
# 1和3行

# 题目2（操作题）
# load_file(filename)：

# 用上一个知识点学的 os.path.exists() 检查文件在不在
# 如果不在，打印 "错误：文件不存在"，然后 sys.exit(1)
# 如果在，打印 "文件存在，继续处理"

# 然后调用一次 load_file("不存在的文件.csv")，把代码和运行结果发我。