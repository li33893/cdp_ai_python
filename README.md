# Python 语法速查索引

---

## 网络请求

| 语法 | 干什么的（简单概括） | 在 d 几 |
|------|------------------|--------|
| `requests.get()` | 向网址发 GET 请求，拿回数据 | d1 |
| `params=` | 给请求带查询参数（如 ?limit=100） | d1 |
| `requests.post()` | 向网址发 POST 请求，提交数据 | d3 |
| `payload` | 打包成 dict 的请求体，POST 时发出去 | d3 |
| `resp.headers.get()` | 从响应头里取某个字段（如 retry-after） | d11 |

---

## 文件与数据格式

| 语法 | 干什么的（简单概括） | 在 d 几 |
|------|------------------|--------|
| `json.dumps()` | dict → json 字符串（写文件用） | d2 |
| `json.loads()` | json 字符串 → dict（读回来用） | d3 |
| `pd.DataFrame()` | 把数据变成表格对象 | d2 |
| `.to_csv()` | 把表格存成 csv 文件 | d2 |
| `pd.read_csv()` | 读 csv 文件成表格 | d7 |
| `with open("r")` | 安全地读文件，自动关 | d6 / d8 |
| `with open("a")` | 追加写文件（不覆盖原内容） | d8 |
| `JSONL 逐行读写` | 一行一个 json，适合断点续跑 | d8 |
| `f-string` | 用 `f"{变量}"` 把变量塞进字符串 | d3 |
| `f-string 格式化` | 控制小数位/对齐，如 `f"{x:.2f}"` | d5 |

---

## Python 基础

| 语法 | 干什么的（简单概括） | 在 d 几 |
|------|------------------|--------|
| `list slicing` | 取列表一段，如 `[:3]` 前三、`[-3:]` 后三 | d8 |
| `set()` + 集合运算 | 去重、求并集 `\|`、判成员 `in` | d8 |
| `max(a, b)` / `max(x, key=)` | 取较大值 / 按规则取最大项 | d8 |
| 三元表达式 | `值A if 条件 else 值B`，一行选值 | d8 |
| `enumerate()` | 循环时同时拿到序号和元素 | d8 |
| `列表推导式` | 一行生成新列表 `[x for x in ...]` | d4 |
| `嵌套字典` | 字典套字典，多层取值 | d4 |
| `None 运算符` | 处理空值，如 `x or 默认值` | d4 |
| `lambda` | 一次性的小匿名函数 | d5 |
| 函数默认值 | 参数不传时用的兜底值 | d5 |
| `str` 方法 | 字符串处理（大小写、查找、替换等） | d5 |
| `tuple` | 不可改的有序序列 | d5 |
| `import` | 引入别的库/模块 | d6 |
| `if __name__=="__main__"` | 只在直接运行时执行的入口 | d6 |
| `sorted(key=)` + `.items()` | 排序（可自定义规则）/ 遍历字典 | d10 |
| `zip()` | 把多个列表按位置配对 | d11 |
| 函数类型注解 | `def f(x: str) -> dict:` 标参数/返回类型 | d11 |
| `**` 幂运算符 | 求幂，如 `2 ** 3 = 8`（指数退避用） | d11 |
| `time.time()` | 取当前时间戳，用来计时 | d11 |
| `Counter` | 快速统计每个元素出现次数 | d12 |

---

## 异常处理

| 语法 | 干什么的（简单概括） | 在 d 几 |
|------|------------------|--------|
| `try/except` | 出错不崩溃，捕获并处理 | d1 / d3 |
| 多重 except | 不同错误类型分开处理 | d8 |
| `assert` | 断言条件为真，否则报错（自查用） | d8 |

---

## pandas

| 语法 | 干什么的（简单概括） | 在 d 几 |
|------|------------------|--------|
| `~` 布尔取反 | 把条件反过来（True↔False） | d9 |
| `is` / `in` | 判断是否为同一对象 / 是否包含 | d9 |
| `df.loc[mask, col]` | 按条件+列名取/改数据 | d9 |
| `.value_counts()` | 统计每个值出现多少次 | d9 |
| `.sort_values(ascending=)` | 按值排序 | d9 |
| `.drop_duplicates(subset=)` | 按某列去重 | d9 |
| `.reset_index(drop=True)` | 重排行号 | d9 |
| `.fillna()` | 填补空值 | d9 |
| `.notna()` | 判断非空 | d9 |
| `.tolist()` | 转成 Python 列表 | d9 |
| `.astype()` | 转类型（bool/int/str） | d9 |
| `.str` accessor | 对整列字符串操作（.upper/.strip等） | d9 |
| `.mean()/.median()/.std()` | 均值/中位数/标准差 | d9 |
| `pd.concat([...])` | 把多个表格拼起来 | d10 |
| `.sample(n=, random_state=)` | 随机抽样（可固定种子复现） | d10 |
| `.merge(on=, how=)` | 按某列合并两个表 | d10 |
| `.dropna(subset=)` | 删除指定列有空值的行 | d10 |
| `.rename(columns={})` | 改列名 | d10 |
| `.isin()` / `~.isin()` | 判断值是否在某集合内 / 不在 | d10 |
| `pd.crosstab()` | 生成交叉统计表 | d10 |
| `.reindex(fill_value=0)` | 重排行列，缺的补默认值 | d10 |
| `.value_counts().index[0]` | 取出现最多的那个值 | d10 |
| `.iloc[0]` | 按位置取第几行 | d10 |

---

## 正则

| 语法 | 干什么的（简单概括） | 在 d 几 |
|------|------------------|--------|
| `re.search()` | 在文本里查找是否匹配某模式 | d2 |
| `re.compile()` | 预编译正则，重复用更快 | d2 |

---

## 工程工具

| 语法 | 干什么的（简单概括） | 在 d 几 |
|------|------------------|--------|
| `argparse` | 让脚本能接收命令行参数 | d11 |
| `os.path` 四件套 | 拼路径/取文件名/去扩展名/判存在 | d11 |
| `os.environ.get()` | 读环境变量（如 API key） | d11 |
| `sys.exit(1)` | 主动退出程序并返回状态码 | d11 |

---

## numpy + sklearn（d12）

| 语法 | 干什么的（简单概括） | 在 d 几 |
|------|------------------|--------|
| `np.array()` | 建 numpy 数组，支持整体运算 | d12 |
| 布尔数组运算 | `r1 == r2` 逐元素比较返回数组 | d12 |
| `np.zeros((m,n))` | 生成全 0 数组 | d12 |
| `np.sum()` | 求和 | d12 |
| `np.sqrt()` | 开方 | d12 |
| `np.trace()` | 矩阵对角线之和 | d12 |
| `axis` 参数 | 控制按行(1)还是按列(0)算 | d12 |
| 二维索引 `cm[i][j]` | 取二维数组第 i 行 j 列 | d12 |
| `cohen_kappa_score` | 算 Cohen's Kappa 一致性 | d12 |
| `confusion_matrix` | 生成混淆矩阵 | d12 |

