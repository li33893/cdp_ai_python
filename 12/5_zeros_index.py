"""

01_np.zeros() —— 建一个全是 0 的矩阵
import numpy as np
m = np.zeros((2, 3), dtype=int)
print(m)
输出:
[[0 0 0]
 [0 0 0]]
拆开看:

(2, 3) 是形状:2 行 3 列。注意它是一个括号包起来的 (行, 列),是一个整体传进去的。
dtype=int 指定格子里是整数。不写的话默认是小数 0.,写了就是整数 0。

用途:你想先建一个空表格,之后再往里填数,就用它开个全 0 的架子。

02_二维索引 m[i][j] —— 定位到第 i 行第 j 列那一个格子
m = np.array([[10, 20, 30],
              [40, 50, 60]])
print(m[0][2])   # 30
print(m[1][0])   # 40

m[0][2]:第 0 行、第 2 列 → 30。
m[1][0]:第 1 行、第 0 列 → 40。

行列都从 0 开始数(不是从 1)。第一个方括号选行,第二个方括号选列。

给某个格子加数:m[i][j] += 1
m = np.zeros((2, 2), dtype=int)
m[0][1] += 1
m[0][1] += 1
print(m)
输出:
[[0 2]
 [0 0]]
第 0 行第 1 列那个格子,被加了两次 1,变成 2。这就是「数数」的动作:每遇到一次某种情况,就往对应格子 +1。

"""

# 题1(预测题,先不要运行)
import numpy as np
m = np.zeros((3, 2), dtype=int)
m[0][0] += 1
m[2][1] += 1
m[2][1] += 1
print(m)
# 打印出的矩阵长什么样?(提示:先想它是几行几列,全 0,再改动几个格子)
# 1 0
# 0 0
# 0 2


# 题2(操作题)
# 用 np.zeros() 建一个 2 行 2 列的全 0 整数矩阵。然后用 += 1 把它改成下面这样并打印:
# [[0 1]
#  [3 0]]

matrix = np.zeros((2, 2), dtype=int)
matrix[0][1] += 1
matrix[1][0] += 3
print(matrix)