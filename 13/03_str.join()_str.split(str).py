"""

.join()

一句话
把一个列表用某个连接符拼成一个字符串。
形式
"连接符".join(列表)
连接符是你想夹在元素中间的东西，写在前面；列表在括号里。

例子
words = ["a", "b", "c"]

" | ".join(words)     # → "a | b | c"      中间夹 " | "
", ".join(words)      # → "a, b, c"        中间夹 ", "
"".join(words)        # → "abc"            什么都不夹
"\n".join(words)      # → "a\nb\nc"        中间夹换行 → 三行
连接符只夹在中间，首尾不加。三个元素 → 两个连接符。

一条硬规则
列表里必须全是字符串。有数字会报错：
" | ".join([1, 2, 3])              # ✗ TypeError
" | ".join(["1", "2", "3"])        # ✓ "1 | 2 | 3"
" | ".join(str(x) for x in [1,2,3]) # ✓ 先转成字符串
所以拼混淆矩阵那行数字时，得先把每个数 format 成字符串（f"{cm[i][j]:>{col_w}}" 出来的就是字符串），再 join。
和「生成器」配合
join 括号里不一定是现成的列表，可以是一个「边生成边喂」的表达式：
labels = ["Habitual", "Episodic", "NM"]

" | ".join(f"{l:>8}" for l in labels)
# 先把每个 l 格式化成宽 8 的字符串，再用 " | " 连起来
# → "Habitual | Episodic |       NM"
f"{l:>8}" for l in labels 这部分逐个吐出格式化后的字符串，join 把它们串起来。这就是你写表格每一行要用的。

两个方向反过来的操作（别搞混）
操作干嘛例子.join()列表 → 字符串（合）`".split()字符串 → 列表（拆）"a,b,c".split(",") → ["a","b","c"]
你现在只用 join。split 提一嘴，混淆矩阵不用。

"""